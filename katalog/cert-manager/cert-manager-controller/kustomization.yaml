---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cert-manager

commonLabels:
  app: cert-manager
  app.kubernetes.io/name: cert-manager

resources:
  - crd.yml
  - deploy.yml
  - issuer.yml
  - ns.yml
  - rbac.yml
  - sm.yml
